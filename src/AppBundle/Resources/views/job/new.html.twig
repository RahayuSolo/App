{% extends 'AppBundle::base.html.twig' %}


{% block body %}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h3 align="center">Job creation</h3>

                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Create" />
                {{ form_end(form) }}



            <button type="button" class="btn btn-default">
                    <a href="{{ path('job_index') }}">Back to the list</a>
            </button>
                
                
        </div>
    </div>
<div>
{% endblock %}
